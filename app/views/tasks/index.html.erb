<div class="container">
<div class="page-header">
  <h1>Tasks <%=link_to "New Task", new_project_task_path(@project), class: "btn btn-info pull-right" %></h1>
</div>

<table class="table">
  <thead>
    <th>Description</th>
    <th>Due date</th>
  </thead>

  <tbody>
        <tr>
        <% @tasks.each do |task| %>
          <td>
            <% if task.is_complete %>
             <s><%= link_to task.description, project_tasks_path(task) %></s>
            <% else %>
            <%= link_to task.description, project_tasks_path(task) %>
            <% end %>
          </td>

          <td>
            <% if task.due_date %>
            <%= task.due_date.strftime("%m/%d/%Y") %>
            <% else %>
            <% end %>
          </td>

          <td class="text-right">
              <%=link_to "Edit", edit_project_task_path(@project, task), class:"btn btn-info" %>
              <%=link_to "Destroy", project_task_path(@project, task), class: "btn btn-danger", method: :delete %>
          </td>
          </tr>
    <% end %>
  </tbody>
</table>
</div>
