<div class="page-header">
  <h1>Projects <%=link_to "New Project", new_project_path, class: "btn btn-info pull-right" %></h1>
</div>

<table class="table">
  <thead>
    <th>Name</th>
    <th>Tasks</th>
  </thead>
  <tbody>
     <% @projects.each do |project| %>
       <tr>
         <td><%= link_to project.name, project_path(project) %></td>
         <td><%= link_to project.tasks.count, project_path(project) %></td>
       </tr>
     <% end %>
   </tbody>
 </table>

 div class="page-header">
   <h1>Pivotal Tracker Projects</h1>
 </div>

 <div class="">
</div>

 <table class="table">
   <thead>
     <tr>
     <th class= "col-md-3">Name</th>
     <tr>
     </thead>

   <tbody>
     <% if @tracker_projects %>
      <% @tracker_projects.each do |project| %>
        <tr>
          <td>
            <%= link_to project[:name], tracker_project_path(project[:id]) %>
          </td>
        </tr>
      <% end %>
      <% end %>
    </tbody>
  </table>
